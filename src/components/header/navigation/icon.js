import styled from "styled-components"

const Icon = styled.span`
  display: inline-block;
  width: 1em;
  height: 1em;
  margin-right: 0.33em;

  svg {
    fill: currentColor;
  }
`

export default Icon
